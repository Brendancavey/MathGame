@model MathGame.Models.ViewModels.ScoreboardModel

@{
    ViewData["Title"] = "Scoreboard";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
@if (Model.user != null)
{   <h1>Session Details</h1>
    <h2>Your final score: @Model.user.sessionScore</h2>
    <h2>Highest score streak: @Model.user.highestSessionStreak</h2>
    <h2>Highest streak of all time: @Model.user.highestStreak</h2>

}

<h1>Leaderboard</h1>
<table class="table">
    <thead>
        <tr>
            <th>Rank</th>
            <th>Name</th>
            <th>High Score</th>
        </tr>
    </thead>
    <tbody>
        @{int rank = 1;}
        @foreach (var user in Model.allUsers)
        {
            <tr>
                <td>@rank</td>
                <td>@user.name</td>
                <td>@user.score</td>
            </tr>
            rank++;  
        }
    </tbody>
</table>

